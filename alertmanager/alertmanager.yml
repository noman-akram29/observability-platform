route:
  group_by: ['alertname', 'host']
  group_wait: 30s
  group_interval: 5m
  repeat_interval: 1h
  receiver: 'local-webhook'

receivers:
  - name: 'local-webhook'
    webhook_configs:
      - url: 'http://127.0.0.1:5001/'
